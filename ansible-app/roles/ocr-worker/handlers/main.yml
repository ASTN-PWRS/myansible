# roles/fastapi/handlers/main.yml

- name: Reload systemd
  systemd:
    daemon_reload: yes

- name: Restart {{ app_name }} service
  systemd:
    name: "{{ app_name }}"
    state: restarted
    enabled: yes

- name: Reload nginx
  systemd:
    name: nginx
    state: reloaded
