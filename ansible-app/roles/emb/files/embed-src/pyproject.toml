[project]
name = "embed"
version = "0.1.0"
requires-python = "==3.13.*"

dependencies = []

[project.scripts]
embed-binary = "cli:main"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]