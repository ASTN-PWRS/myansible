---
- name: Ensure project directory exists
  ansible.builtin.file:
    path: "~/projects/{{ project_name }}"
    state: directory
    mode: "0755"

- name: Deploy .env file
  ansible.builtin.template:
    src: "templates/.env.j2"
    dest: "~/projects/{{ project_name }}/.env"
    mode: "0600"
