[project]
name = "embed"
version = "0.1.0"

dependencies = [
    "sentence-transformers",
]

[project.scripts]
embed-binary = "embed.cli:main"

[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
