---
- name: Update apt cache
  apt:
    update_cache: yes
  tags:
    - nginx
    - nginx_install
    - install

- name: Install nginx
  apt:
    name: nginx
    state: present
  tags:
    - nginx
    - nginx_install
    - install
