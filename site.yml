- hosts: local
  become: yes
  roles:
    - common # OS 基本設定
    - php # PHP-FPM + php.ini
    - nginx # nginx install
    - node # Node.js / nvm
    - composer # composer install
    - python # python + uv
    - postgresql # PostgreSQL install + config + backup
    - garnet # Garnet
